# This file was auto-generated by AzWebApp > Deployment > Deployment Center
name: Web API Build and Deploy Pipeline

env:
  AZURE_WEBAPP_NAME: "app-speedruns" # Must match Web App resource name
  AZURE_WEBAPP_PACKAGE_PATH: "./MrJobs.WebApi"
  AZURE_WEBAPP_RUNTIME_STACK: "DOTNETCORE|9.0"
  DOTNET_VERSION: "9.x" # This should match the runtime stack
  APPROVERS: "jjjjony" # GH usernames comma-separated

on:
  push:
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    name: Build Stage
    runs-on: windows-latest
    permissions:
      contents: read #This is required for actions/checkout
    steps:
      - uses: actions/checkout@v4
      - name: Set up .NET Core
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
      - name: Build with dotnet
        run: dotnet build "${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/MrJobs.WebApi.csproj" --configuration Release
      - name: dotnet publish
        run: dotnet publish "${{ env.AZURE_WEBAPP_PACKAGE_PATH }}/MrJobs.WebApi.csproj" -c Release -o "${{env.DOTNET_ROOT}}/myapp"
      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v4
        with:
          name: .net-app
          path: ${{env.DOTNET_ROOT}}/myapp

  manual-approval:
    name: Approval
    runs-on: ubuntu-latest
    needs: build
    if: success()
    permissions:
      issues: write
    steps:
      - name: Await Manual Approval
        uses: trstringer/manual-approval@v1.9.0
        with:
          secret: ${{ github.TOKEN }}
          approvers: ${{ env.APPROVERS }}
          minimum-approvals: 1

  deploy:
    name: Deploy Stage
    runs-on: windows-latest
    needs: manual-approval
    if: success()
    environment:
      name: "Production"
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}
    permissions:
      id-token: write # This is required for requesting the JWT
      contents: read # This is required for actions/checkout
    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v4
        with:
          name: .net-app
      - name: Login to Azure
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZUREAPPSERVICE_CLIENTID_A7C976CED4E3453387EF337A2411A2AC }}
          tenant-id: ${{ secrets.AZUREAPPSERVICE_TENANTID_85BF8C9AC6694FBB8F13927E9D5FF53B }}
          subscription-id: ${{ secrets.AZUREAPPSERVICE_SUBSCRIPTIONID_63D7095076824EE5B858CCAD6E737B8F }}
      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v3
        with:
          app-name: ${{ env.AZURE_WEBAPP_NAME }}
          slot-name: "Production"
          package: . # Corrected package path
          stack: ${{ env.AZURE_WEBAPP_RUNTIME_STACK }}
